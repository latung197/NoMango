namespace Wcs.Okamura.Enums;

public enum ConveyorRunState
{
    Unknown = 0,
    Stopped = 1,
    Running = 2
}

public enum ConveyorDirection
{
    Unknown = 0,
    Forward = 1,
    Reverse = 2
}

public enum ConveyorCommandType
{
    Start = 1,
    Stop = 2
}